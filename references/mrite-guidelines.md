# 数学建模竞赛求解+论文自动生成模板

## ⛔ 全自动模式（最高优先级）

**本模板设计为全自动无人值守运行。以下规则优先级高于一切：**

在 Codex 中，本节表示“尽量连续推进”的工作风格，不覆盖系统指令、用户最新要求、AGENTS.md、权限审批、联网/文件安全规则；遇到这些上位约束时必须遵守上位约束。

1. **禁止任何形式的用户交互**：不询问、不确认、不等候、不提示选择
2. **禁止中断等待**：遇到任何决策点自行判断并继续，不做 `ask` 类操作
3. **从头到尾一次性完成**：读取输入 → 生成计划 → 逐题求解 → 撰写论文 → 编译 PDF，中间不停
4. **编译后自动排版优化**：无需询问，直接执行
5. **即使遇到不确定性也自行选择最优方案继续**，不做任何停顿

---

## 项目结构

```
竞赛模板/
├── CLAUDE.md
├── 题目/                  ← 赛题文件（PDF或DOCX）
├── 数据/                  ← 附件数据（xlsx/docx）
├── 解题要求/              ← 可选：解题思路、老师要求、方法偏好、格式要求；可为空
├── 求解/                  ← AI自动生成
│   ├── 求解计划.md
│   ├── 预处理数据.csv
│   ├── 问题一/
│   │   ├── 问题一_<描述>.py
│   │   ├── 图片/
│   │   └── 结果/
│   ├── 问题二/ ...
│   └── 问题N/
└── 论文/                  ← AI自动生成
    ├── 论文.tex
    ├── format.cls
    ├── fonts/
    ├── 0.摘要.tex ~ 4.符号说明.tex
    ├── 5.模型的建立与求解.tex
    ├── 5.1.问题1的建立求解.tex    ← 主文件，\input 两个子文件
    ├── 5.1.1.分析与准备.tex          ← 具体分析 + 流程图 + 模型准备
    ├── 5.1.2.建模与求解.tex         ← 模型建立 + 模型求解
    ├── 6.模型检验.tex ~ 8.模型改进推广.tex
    ├── 9.参考文献.tex
    └── 10.附录.tex
```

**文件命名**：代码、图、CSV、tex文件一律中文。图存 `求解/问题X/图片/`，论文通过 `../求解/问题X/图片/xxx.png` 引用。CSV/结果存 `求解/问题X/结果/`。论文目录只放tex+cls+fonts。

---

## 一、执行流程

触发：用户说"开始求解"/"求解这个题目"/"按规则求解"。

**全自动模式：全程无需用户确认，不询问、不等待、不中断。从读取输入到论文编译完成一气呵成。**

### Modex 风格阶段门

完整求解必须拆成六个阶段，每个阶段都留下可检查产物。不要把读题、建模、写代码和写论文混在一起推进。

| 阶段 | 目标 | 必须产物 | 进入下一阶段前检查 |
|------|------|----------|--------------------|
| 题目分析 | 明确每问任务、输入、输出、约束和评价标准 | `求解/求解计划.md` 中的问题拆解、数据依赖、输出清单 | 每问都有任务类型、目标变量或目标函数、约束、预期图表/表格 |
| 建模设计 | 为每问选主方法和备选方法 | 方法匹配表、变量表、公式草案、异常预案 | 每个方法能对应题目要求和数据字段，不出现只写模型名无理由 |
| 代码求解 | 先计算、后绘图，生成可复查结果 | 每问脚本、`结果/` 表格、`图片/` 图、运行日志或打印统计量 | 脚本可重复运行，输出路径固定，结果表能支撑论文结论 |
| 题型检查 | 发现数模常见硬伤 | 每问检查摘要，写入结果表或计划文档 | 通过对应的优化/预测/评价/机理/一致性/常识检查 |
| 论文写作 | 从已验证产物生成论文 | modular TeX 文件、图表引用、公式和变量说明 | 所有结论可追溯到结果表/图片/公式/检查输出 |
| 编译质检 | 得到可提交 PDF | `论文.pdf`、编译日志、warning 修复记录 | Error 为 0，引用和目录正常，摘要 1 页，无明显溢出和大空白 |

若时间不足，优先保证阶段门证据链完整：题目分析、可运行代码、关键结果表、论文核心结论和 PDF 编译，比堆砌更多图表更重要。

### Step 0：读取输入
1. 读 `题目/`（PDF用PyPDF2，DOCX用python-docx）
2. 读 `解题要求/`（可为空）。若该目录存在文件，先读取所有可用的 md/txt/pdf/docx 文档，提取外部要求、指定方法、禁止方法、输出格式、评分偏好、论文风格、字数页数、必须回答的额外问题，并写入 `求解/求解计划.md` 的“外部要求”部分。若目录为空，记录“无额外解题要求”，继续执行，不等待用户补充。
3. 读 `数据/` 下所有xlsx/docx，**全量读取**，不只前几行。仔细检查：
   - 每个sheet的列数是否每行一致
   - 每列数据类型是否统一（数值列里有没有混文字）
   - 是否有合并单元格、多级表头、空行分隔、底部备注行
   - 是否有隐藏sheet
4. 打印数据总览：每个sheet的 行数×列数、列名、数据类型、异常行（如有）
5. 自动识别问题数量N

### Step 1：求解计划
读题后，写 `求解/求解计划.md`，写完直接开干。六章结构：
- **题目总体方向**（一整段）：问题类型、核心任务、数据规模、研究递进逻辑
- **外部要求**：来自 `解题要求/` 的约束和偏好。若目录为空，写“未提供额外解题要求”。若外部要求与赛题或系统/用户约束冲突，遵守上位约束，并在计划中记录取舍。
- **各题求解思路**（每问含具体分析+建模+方法匹配表）：文章风格，不分点。具体分析写输入/输出/难点/定位，建模写核心公式+方法选择理由，末尾附表列出主方法、备选方法、关键算法/工具。方法表必须包含题型、输入字段、输出指标、主方法、备选方法、验证方式。
- **各题输出标准**（含图表规划+预期输出）：每问必须产出的图表、表格、数值结果清单
- **操作步骤**（含依赖顺序）：每问从加载→预处理→建模→求解→输出的完整链路，标注数据在问题间的流转
- **文件清单**：代码/图/CSV文件名（中文）
- **题型检查清单**：按优化、预测/分类、评价、机理、统计分析、建议总结列出本题需要通过的检查
- **异常预案**：数据缺失、模型不收敛、指标冲突、约束不可行、中文字体缺失、LaTeX 浮动体失控等备选处理

### Step 2：逐题求解
1. 按N创建 `求解/问题一/图片/` ~ `求解/问题N/图片/`（如不存在），创建 `求解/问题一/结果/` ~ `求解/问题N/结果/`（如不存在）
2. 每个问题一个py文件，中文命名，放在 `求解/问题<序号>/`
3. py文件内部**先算后画**：先完成所有计算得到数值结果，保存核心结果 CSV/Excel，打印每组数据的 min/max/mean/std/CV/amplitude（供论文引用），完成题型检查后再画图（每题至少 4-6 张图，覆盖主要分析维度，多多益善）
4. 问题1输出 `求解/预处理数据.csv` 供后续共用
5. 如有 `Glyph missing from font` 等中文渲染警告，修正字体配置后重跑；如脚本报错，修复后重跑，通过后再下一问
6. **灵敏度分析前置**：在求解阶段就测试关键参数（如树的数量、学习率、正则化系数）对结果的影响，记录最佳参数组合，供论文第6章直接引用
7. 每个脚本末尾输出一段“检查摘要”：数据规模、关键结果、异常值处理、模型指标、是否通过题型检查。该摘要可以打印到终端，也可以保存到 `结果/检查摘要.txt`。

### Step 2.5：题型检查矩阵

逐题求解后、写论文前必须做题型检查。根据每问类型选择对应行，不相关项可跳过，但不能跳过一致性和常识检查。

| 题型 | 必查项 | 典型输出 |
|------|--------|----------|
| 优化类 | 目标函数方向是否正确；决策变量范围是否合理；硬约束是否满足；软约束惩罚是否解释清楚；最优解是否优于基线方案 | 约束残差表、目标值对比表、参数灵敏度表 |
| 预测/分类类 | 训练/测试划分或交叉验证；指标选择是否匹配任务；是否有数据泄漏；异常值和缺失值处理是否在训练集内拟合；预测区间或误差解释 | 指标表、残差图、特征重要性或系数表 |
| 评价类 | 指标同向化；无量纲化；权重和为 1；正负指标处理；排名对权重扰动是否稳定；评价结论是否和原始数据直觉一致 | 权重表、综合得分表、排名稳定性表 |
| 机理/物理类 | 单位量纲一致；参数范围有来源或解释；边界条件合理；极端输入下结果不违背常识；仿真步长或数值方法稳定 | 参数表、边界测试表、极端情景图 |
| 统计/数据分析类 | 样本量、缺失率、异常率；分组口径一致；相关不等于因果；显著性或置信区间不过度解释 | 数据概览表、相关性表、分组统计表 |
| 建议总结类 | 每条建议对应前文结果；建议可执行；不引入前文没有证明的新判断 | 建议-证据对应表 |
| 全部题型 | 问题间共享数据口径一致；图表编号和文件路径稳定；论文中的数值与结果表一致；摘要结论不夸大 | 一致性核对清单 |

常见硬伤预防：

- 不要只给图不给结果表。论文中的关键数值应来自 `结果/` 下的表。
- 不要只给模型名不给公式。至少写出目标、变量、约束、损失函数或评价公式。
- 不要让后续问题重新清洗一套不同口径的数据；共享数据应来自问题1的预处理输出。
- 不要把预测类问题写成解释性散点图分析；必须有可复查的预测指标。
- 不要把评价类问题只写成主观打分；必须有指标、权重、同向化和综合得分。
- 不要把优化类问题只写成排序；必须明确决策变量和约束。

### Step 3：写论文
按第二章规范逐章写tex。
- `5.模型的建立与求解.tex` 按N生成 `\input{5.X}`
- 每个 `5.X.问题X的建立求解.tex` 内部拆为2个子文件：`5.X.1.分析与准备.tex`（具体分析+流程图+模型准备）和 `5.X.2.建模与求解.tex`（模型建立+模型求解）。从 `5.1.1~5.1.2` 模板复制改名
- 拆分原则：模型建立和模型求解必须在一起（求解大量引用建立的公式/变量/参数），拆开适得其反

### Step 4：编译
```bash
cd 论文
xelatex -interaction=nonstopmode 论文.tex
xelatex -interaction=nonstopmode 论文.tex
```
`grep -c 'Error' 论文.log` 必须为0。有 Error 则修复重编译直到 0。

### Step 5（自动）：排版优化

编译0错误后，自动执行排版优化直到所有问题清除：
1. `grep 'Overfull\|Underfull\|Float too large' 论文.log`
2. 根据 warning 定位对应 tex 文件的具体行
3. 修复（缩短表格文字、调整图表尺寸、收紧段落等）
4. `xelatex ×2` 重编译，重新检查
5. 直到日志干净、排版紧密、无溢出无大面积空白、图表正常
6. 核对 `LaTeX Warning: Reference ... undefined`、`Citation ... undefined`、`There were undefined references`。有未定义引用必须修复。
7. 核对 `论文.aux` 中摘要结束页码，确保摘要为 1 页。
8. 打开或渲染最终 PDF，快速检查首页、摘要、目录、图表密集页和参考文献页是否明显错位。

**全程无需用户确认，自动执行。**

---

## 二、论文规范

### 通用规则

- **模板为默认结构，如与题目实际内容不适配，AI自行调整**，不硬套
- tex文件中只写可编译的LaTeX代码，不可出现 `...（提示语）` 等模板指令
- 正文一律自然段落，禁止分点符号（1. 2. 3. 或 ● ○ 等）
- 禁止正文加粗（`\textbf` 仅用于摘要中的"对于问题X"和问题重述中的"问题N："）
- 图宽 `0.8\textwidth`，表宽 `\textwidth`
- 所有图表必须被引用（"如图X所示""如表X所示"），图表标题/caption/坐标轴全中文
- 全文任何位置（章与章、节与节、子节与子节之间）一律不加 `\newpage`，内容自然接续，禁止人为断页
- 图文件不画 `set_title()`，标题由 `\caption{}` 承担
- **表格格子文字一行显示，不换行**

### 论文.tex 主文件结构

```latex
\PassOptionsToPackage{quiet}{xeCJK}
\documentclass[withoutpreface,bwprint]{format}
\usepackage{etoolbox}
\usepackage{ctex}
\BeforeBeginEnvironment{tabular}{\zihao{-5}}
\usepackage[framemethod=TikZ]{mdframed}
\usepackage{url}
\usepackage{array}
\usepackage{tabularx}
\usepackage{longtable}
\newcolumntype{C}{>{\centering\arraybackslash}X}
\newcolumntype{R}{>{\raggedleft\arraybackslash}X}
\newcolumntype{L}{>{\raggedright\arraybackslash}X}

\renewcommand{\textbf}[1]{{\song\bfseries #1}}

\usepackage{tikz}
\usetikzlibrary{arrows.meta}
\tikzset{
  box/.style={rectangle, draw, minimum width=3.2cm, minimum height=0.9cm, align=center},
  arrow/.style={thick, -{Stealth}}
}

\title{论文标题}

\begin{document}

\maketitle
\thispagestyle{empty}

\input{0.摘要.tex}
\thispagestyle{empty}

\tableofcontents
\thispagestyle{empty}
\newpage

\setcounter{page}{1}

\input{1.引言.tex}

\input{2.总体分析.tex}

\input{3.模型假设.tex}
\input{4.符号说明.tex}

\input{5.模型的建立与求解.tex}

\input{6.模型检验.tex}

\input{7.模型评价.tex}

\input{8.模型改进推广.tex}

\input{9.参考文献.tex}

\input{10.附录.tex}
\end{document}
```

---

### 各章节详细规范

#### 0. 摘要

**字数硬约束**：

| 段落 | 字数 | 说明 |
|------|------|------|
| 开头段 | ≤30字 | 固定 |
| 结尾段 | ≤20字 | 固定 |
| **总计** | **≤900字** | **不可超过** |
| 页数 | 必须=1页 | 编译后验证 |

**各问题字数按类型弹性分配**（总计扣除头尾后≈850字在各问题间分配）：

| 问题类型 | 摘要占比 | 说明 |
|----------|----------|------|
| 建模求解型（优化/预测/分类/评价） | 高（≈30-35%） | 方法+求解+结果占篇幅 |
| 数据分析型（统计/探索/可视化） | 中（≈20-25%） | 处理+发现+结论 |
| 建议总结型（建议/总结/改进） | 低（≈10-15%） | 一笔带过即可 |

例：4问含2个建模型+1个分析型+1个建议型 → 建模型各≈250字、分析型≈180字、建议型≈120字、头尾50字=850字。 |

**直接复制以下模板框架，每问选类型替换 `【】`：**

```latex
\begin{abstract}
本文针对【题目背景】问题，提出了基于【主要方法】的系统解决方案。

【每问从这里复制，选对应类型模板填入】

综上，本文为【领域】问题提供了高效解决方案。

\keywords{【关键词1】；【关键词2】；【关键词3】；【关键词4】；【关键词5】；【关键词6】}
\label{abstract:end}
\end{abstract}
```

**每问三种类型，对号入座**：

**① 有数据**（题目提供附件数据，含预处理→建模→求解全流程，预测/分类/优化/评价等均归此类）：
```
\textbf{针对问题X：}本问是【类型】问题，需【要求】。首先，对附件数据预处理，提取【特征】，【清洗/标准化等操作】。接着，【变量筛选/降维/参数选择】。然后，采取【模型】进行【求解过程】。最后，【对比/验证/排序】，给出【输出】。结果讨论显示，\textbf{【数值结果】}，模型有效地【评价】。
```

**② 无数据**（无附件数据，直接建立机理/方程/优化模型，仿真/推导/理论分析等均归此类）：
```
\textbf{针对问题X：}本问是【类型】问题，需【要求】。首先，分析【机理/物理过程/目标约束】，建立【方程/动力学/优化模型】。接着，设定【参数/初始条件】。然后，通过【数值方法/仿真工具/求解算法】进行求解，得到【结果】。结果讨论显示，\textbf{【数值结果】}，模型有效地【评价】。
```

**③ 建议型**（基于前文结果给出建议/策略/总结，一笔带过）：
```
\textbf{针对问题X：}本问是建议总结问题，需【要求】。基于前文分析，从【角度1】和【角度2】出发，给出【建议数量】条建议。核心结论为\textbf{【要点】}。
```

**一页验证（编译后必做）**：

```bash
# 编译论文后，检查摘要是否超出一页
PAGE=$(grep 'abstract:end' 论文/论文.aux | grep -oE '\{[0-9]+\}' | head -1 | tr -d '{}')
if [ "$PAGE" -gt 1 ]; then
  echo "摘要超出一页（第${PAGE}页），需缩减"
fi
```

若 `PAGE > 1` 则摘要超出，按比例缩减各问题段字数后重新编译，直到 `PAGE=1`。

**禁止**：公式、模型检验、超字数、摘要末尾加 `\newpage`。

#### 1. 引言

| 部分 | 格式 | 要求 |
|------|------|------|
| 问题背景 | 文字，2-3段 | 宏观背景→问题来源/原因→当前困境→解决渴求，不分点 |
| 研究意义（可选） | 文字，1段 | 社会意义 + 科研意义 |
| 问题重述 | `\textbf{问题N：}` 每问≤2行 | 问题本质缩写，删背景。不加分点符号 |

#### 2. 总体分析

| 部分 | 格式 | 要求 |
|------|------|------|
| 文字 | 三段式 | 第1段：一句话总体表达。第2段：针对问题X建立了XX来解决XX（逐问串联）。第3段：总结各问题之间的递进关系 |

不画流程图。

#### 3. 模型假设

- 两段式：第1段"为简化问题，本文做出以下假设："
- itemize，每条 `\textbf{假设N：}`，≤2行，数量=问题数

#### 4. 符号说明（全论文表格统一模板）

**所有表格统一使用 `longtable` + `>{\centering\arraybackslash}p{}`，总宽=`\textwidth`，各列居中可跨页，宽度按内容比例分配。**

```latex
\begin{longtable}{>{\centering\arraybackslash}p{0.18\textwidth}>{\centering\arraybackslash}p{0.78\textwidth}}
  \caption{表名（中文）}
  \label{tab:标签} \\
  \toprule
  列1 & 列2 \\
  \midrule
  \endfirsthead
  \caption{表名（续）} \\
  \toprule
  列1 & 列2 \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  内容 & ... \\
  ... & ...
\end{longtable}
```

**列宽规则（强制）**：

`\tabcolsep` 每列左右各6pt，N列共12N pt，超 `\textwidth` 则右侧溢出。因此各 `p{比例\textwidth}` 比例总和**不可等于1.0**，须按下表预留余量：

| 列数 | 比例总和 | 示例分配（左侧标签列窄→右侧内容列宽） |
|------|----------|----------------------------------------|
| 2列 | **0.96** | `{0.18+0.78}` |
| 3列 | **0.92** | `{0.16+0.16+0.60}` |
| 4列 | **0.88** | `{0.12+0.12+0.12+0.52}` |
| 5列 | **0.84** | `{0.12+0.18+0.18+0.18+0.18}` |
| 6列 | **0.80** | `{0.10+0.14+0.14+0.14+0.14+0.14}` |
| 7列 | **0.76** | `{0.10+0.11+0.11+0.11+0.11+0.11+0.11}` |
| 8列 | **0.72** | `{0.09+0.09+0.09+0.09+0.09+0.09+0.09+0.09}` |
| 9列 | **0.68** | `{0.08+0.075+0.075+0.075+0.075+0.075+0.075+0.075+0.075}` |
| 10列 | **0.64** | `{0.064+0.064+0.064+0.064+0.064+0.064+0.064+0.064+0.064+0.064}` |

通式：比例总和 = 1.04 − 0.04 × N（N为列数）。示例中左侧标签列取窄值，各列可据实际最长内容微调，但**总和必须等于该列数对应的强制值**。

- 所有列居中：`>{\centering\arraybackslash}` 不可省略，否则 `\\` 被干扰导致 `Misplaced \noalign`

**表格规则（强制）**：
- **全论文所有表格（符号说明、算法对比、预处理对比、模型结果、检验分析等一切表格）必须统一使用上述符号说明处定义的 `longtable` + `>{\centering\arraybackslash}p{}` 样式，不得使用 `tabular`、`tabularx` 等其他表格环境**
- `\caption{}` 在 `\label{}` 之前，`\label{}` 后紧跟 `\\`
- `\endfirsthead` / `\endhead` / `\endfoot` 固定结构，跨页时自动打表头
- `\toprule/\midrule/\bottomrule` 固定，`\endfoot` 含 `\bottomrule`，表末不再重复写
- 格子文字一行显示，禁止换行
- 编译后 `grep 'Overfull.*hbox'` 复查

#### 5. 模型的建立与求解

主文件按N动态生成，N>4新建，N<4删除：

```latex
\section{模型的建立与求解}

\input{5.1.问题1的建立求解.tex}
% 按N增减：复制5.1并改名5.2~5.N，在此逐个\input
```

每个 `5.X.问题X的建立求解.tex` 拆为2个子文件，通过 `\input{}` 引入，各小节自然接续，不加 `\newpage`：

```latex
% 5.X.问题X的建立求解.tex
\subsection{问题X的模型的建立和求解}
\input{5.X.1.分析与准备.tex}
\input{5.X.2.建模与求解.tex}
```

模板文件（论文目录预置）：`5.1.1.分析与准备.tex`、`5.1.2.建模与求解.tex`，每个问题从模板复制改名。
总结建议类问题不分小节，自然段落。

##### 5.X.1 分析与准备

包含**具体分析**和**模型准备**两部分。

**具体分析**（文字 + 流程图）：

| 要求 | 内容 |
|------|------|
| 三段式 | 第1段：针对问题X + 问题类型 + 大致方法 |
| | 第2段：首先…接着…然后…最后…串联完整步骤，不换段 |
| | 第3段：具体分析流程如图X所示 |
| 流程图 | 固定9框模板，**只替换{}中文字，其余代码一字不改**。每框≤7字 |
| 禁止 | 不讲检验、不讲结果（还没求解） |

流程图固定模板（严格逐字复制，只替换{}中文字，每框≤7字）：

```latex
\begin{figure}[ht]
  \begin{center}
    \begin{tikzpicture}[x=1cm, y=0.7cm]
      \node[box] (n11) at (0, 0) {...};
      \node[box] (n12) at (5, 0) {...};
      \node[box] (n13) at (10, 0) {...};
      \node[box] (n22) at (5, -3) {...};
      \node[box] (n21) at (0, -3) {...};
      \node[box] (n23) at (10, -3) {...};
      \node[box] (n31) at (0, -6) {...};
      \node[box] (n32) at (5, -6) {...};
      \node[box] (n33) at (10, -6) {...};
      \draw[arrow] (n11) -- (n12);
      \draw[arrow] (n12) -- (n13);
      \draw[arrow] (n23) -- (n22);
      \draw[arrow] (n22) -- (n21);
      \draw[arrow] (n13) -- ++(0,-1.0) -| (n23);
      \draw[arrow] (n21) -- (n31);
      \draw[arrow] (n31) -- (n32);
      \draw[arrow] (n32) -- (n33);
    \end{tikzpicture}
    \caption{问题X分析流程图}
    \label{fig:analysisX_flow}
  \end{center}
\end{figure}
```

**模板规则：**
- 箭头方向/布局/坐标一律不动，只改 `{...}` 里的文字
- 每框不超过7个汉字
- 箭头走向：第1行左→右，第2行右→左，第1行右端向下转入第2行右端，第2行左端向下转入第3行左端，第3行左→右
- label 中 `analysisX_flow` 的 X 替换为问题序号数字

**模型准备**：包含**数据预处理**（仅问题1）和**算法选择**（问题1必写，其他问题视情况）。结构统一为：引文→分析→表格→结论。

**数据预处理**（仅问题1）：

必须包含以下部分，逐段展开：

| 部分 | 内容 | 说明 |
|------|------|------|
| 第1段 | 预处理原因 | 说明原始数据的来源、格式与存在问题（如缺失值、量纲不统一、异常值等），强调预处理的必要性 |
| 第2段 | 数据理解与提取 | 从附件数据中提取关键信息（订单/距离/时间窗/坐标等），列出提取后的特征数量与样本规模 |
| 第3段 | 预处理步骤 | 逐项列出：数据清洗（缺失值处理、异常值检测）→ 标准化/归一化（如Min-Max、Z-score，写公式）→ 特征提取/衍生（如有） |
| 第4段 | 预处理结果 | 展示处理前后的关键统计量对比（min/max/mean/std），可选对比表或图；说明处理后数据的规模与结构 |
| 第5段 | 总结 | 预处理完成，数据已规整，为后续建模提供清洁输入 |

**预处理公式示例：**

Min-Max归一化：
\begin{equation}
x' = \frac{x - x_{min}}{x_{max} - x_{min}}
\end{equation}

Z-score标准化：
\begin{equation}
x' = \frac{x - \mu}{\sigma}
\end{equation}

其中，$x_{min}$、$x_{max}$分别为最小值和最大值，$\mu$为均值，$\sigma$为标准差。

**预处理结果对比表（可选）：**

```latex
\begin{longtable}{>{\centering\arraybackslash}p{0.12\textwidth}>{\centering\arraybackslash}p{0.18\textwidth}>{\centering\arraybackslash}p{0.18\textwidth}>{\centering\arraybackslash}p{0.18\textwidth}>{\centering\arraybackslash}p{0.18\textwidth}}
  \caption{数据预处理前后统计量对比}
  \label{tab:预处理对比} \\
  \toprule
  指标 & 预处理前均值 & 预处理前标准差 & 预处理后均值 & 预处理后标准差 \\
  \midrule
  \endfirsthead
  \caption{数据预处理前后统计量对比（续）} \\
  \toprule
  指标 & 预处理前均值 & 预处理前标准差 & 预处理后均值 & 预处理后标准差 \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  ... & ... & ... & ... & ... \\
\end{longtable}
```

**算法选择**（问题1必写，含表格对比。其他问题如有算法选型讨论也写）：

必须包含以下五个部分，缺一不可：

| 部分 | 内容 | 说明 |
|------|------|------|
| 第1段 | 问题本质定性 | 该问题本质上是一个XXX问题，需要在XXX条件下寻找XXX |
| 第2段 | 候选算法 + 评估维度 | 常用求解算法包括XX、XX、XX等。本节选取N种经典算法，从维度1、维度2、维度3等方面进行评估 |
| 对比表 | `longtable` 五列 `p{}` 格式 | 必须包含具体算法名称（中文+英文缩写）和评估等级（优/良/中/差），不可只写"算法A" |
| 第3段 | 分析对比结果 | 通过上述对比可以看出，XX算法在XX方面表现最优，…因此选择XX算法 |
| 第4段 | 总结+引出 | 综上所述，本节采用XX算法…通过操作1、操作2、操作3等，最终获得… |

算法对比表示例（严格按此格式）：

```latex
\begin{longtable}{>{\centering\arraybackslash}p{0.12\textwidth}>{\centering\arraybackslash}p{0.18\textwidth}>{\centering\arraybackslash}p{0.18\textwidth}>{\centering\arraybackslash}p{0.18\textwidth}>{\centering\arraybackslash}p{0.18\textwidth}}
  \caption{算法能力对比}
  \label{tab:算法对比X} \\
  \toprule
  算法名称 & 全局搜索能力 & 收敛速度 & 约束处理能力 & 求解稳定性 \\
  \midrule
  \endfirsthead
  \caption{算法能力对比（续）} \\
  \toprule
  算法名称 & 全局搜索能力 & 收敛速度 & 约束处理能力 & 求解稳定性 \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  遗传算法（GA） & 优 & 优 & 良 & 良 \\
  蚁群算法（ACO） & 良 & 中 & 优 & 中 \\
  模拟退火算法（SA） & 中 & 良 & 良 & 中 \\
\end{longtable}
```

**注意**：评估维度根据问题类型自行调整（如预测类可用"预测精度/训练效率/可解释性/鲁棒性"），但表头格式不变。格子文字一行显示。

##### 5.X.2 建模与求解

包含**模型建立**和**模型求解**两部分。两者必须在同一文件——求解大量引用建立的公式/变量/参数，拆开会导致符号不一致。

**模型建立**：每个5.X子文件的核心主体，占最大篇幅。结构为**三段式**：

- **第1段（开篇引文）**：点明"针对问题X，本节将从XX角度建立XX模型"，概述建模思路，"具体过程如下"收尾。
- **第2段（中间展开）**：按编号步骤逐步建模，每步一个 `\textbf{步骤N：标题}`，后跟公式推导段。公式之间用文字串联推导逻辑，不能孤立堆砌。每步包含：问题引入→公式建立→推导过程→变量说明→中间结果。
- **第3段（总结桥接）**："综上所述，本节完成了XX模型的建立，得到了目标函数/评价体系/预测模型，接下来将基于此进行求解。"

**中间展开规则：**

| 要求 | 说明 |
|------|------|
| 公式数量 | 每个编号步骤至少6-7个公式，能推导的每一步都写出公式 |
| 推导详细 | 从基本形式出发，逐步展开到最终形式，中间过程不可跳步 |
| 文字串联 | 公式之间用"将式(X)代入式(Y)可得""由XX性质可知""进一步展开得"等过渡 |
| 变量说明 | 每个公式后紧跟变量说明段，解释每个符号含义 |

**各类型中间步骤划分：**

| 类型 | 编号步骤 |
|------|----------|
| 优化类 | 1.目标函数的建立（2-3公式：基本形式→考虑权重→最终形式）→ 2.约束条件的建立（逐约束编号，2.X 时间约束→2.X 容量约束等，每约束1-2公式）→ 3.核心机理建模（专项子模型，如时变函数/能耗函数/动力学方程，3-5公式）→ 4.求解算法建模（算法流程公式+参数表） |
| 评价类 | 1.评价指标体系的构建（指标表+无量纲化公式）→ 2.指标权重确定（熵权法/层次分析法完整推导，包含信息熵公式→权重公式，展示中间计算结果表）→ 3.综合评价方法（TOPSIS/模糊综合评价，完整公式链：加权矩阵→正负理想解→贴近度，至少5-6公式） |
| 分类/预测类 | 1.特征工程（相关性公式+降维推导+特征表）→ 2.模型选定与构建（模型数学表达式完整写出，包含损失函数/优化目标/正则项）→ 3.模型训练与评估（评估指标公式+训练策略描述） |
| 机理分析 | 1.机理分析框架（核心变量关系图/方程组）→ 2.关键变量关系建模（每个关系写公式+推导）→ 3.模型整合与参数确定（联立方程+参数辨识公式） |

**通用规则（不变）：**
- 每个公式用编号标题引出：`\textbf{标题：}`描述 + `\begin{equation}...\end{equation}` + 变量说明段
- 变量说明段格式：`其中，$X$表示...，$Y$表示...。`（自然段，不分点）
- 公式不单独编号，用 `equation` 环境即可

**注意**：优化类必须包含完整的优化模型三要素（目标函数+决策变量+约束条件），公式必须完整写出不可用文字概括。

**模型求解**：引文→表/图→分析。数据结果用表格突出，趋势/对比用图展示。图内只允许：数据、坐标轴标签刻度、图例（全中文）。不出现分点、加粗。

#### 6. 模型检验与分析

| 要求 | 内容 |
|------|------|
| 结构 | 开头引文→二级标题分类→文字解释→表/图→分析 |
| 子节 | **误差分析**（优先5折交叉验证）+ **灵敏度分析**（改输入/参数看输出） |
| 展示 | 结果优先用表格 |
| 自适应 | 如模板结构不适配当前题目，AI根据问题实际内容自行组织，要求输出：文字引入分析 + 表格展示结果 + 文字分析总结 |

#### 7. 模型评价

优点4条 + 缺点2条，按模型写不按问题写，优点多于缺点。itemize格式，`\textbf{优点N：}` / `\textbf{缺点N：}`，每条≤3行。

#### 8. 模型改进与推广

各一段自然段落，不分点。

#### 9. 参考文献

**必须根据论文实际内容生成，禁止保留模板中的示例文献。**

- 每条 `\bibitem{标签}` 的标签用 `作者姓氏+年份+关键词` 格式（如 `\bibitem{behzadian2012topsis}`）
- 国标 GB/T 7714-2015 格式，纯文本一行写完，严禁 `\emph{}` `\textbf{}` `\textit{}` `\underline{}` 等格式命令，删除 DOI 号
- **数量**：8-15 条
- **内容来源**：建模方法（如 sklearn/scipy 相关论文）、数据来源、核心算法原始文献、题目领域相关研究
- **引用要求**：每条 `\bibitem{}` 必须在正文中被 `\cite{}` 引用，正文无引用的条目删掉
- **引用位置**：模型建立（方法原始文献）、模型求解（算法文献）、模型检验（对比方法文献）、引言（领域背景文献）
- 写完论文后，对照正文 `\cite{}` 逐一核对参考文献列表，确保一一对应

#### 10. 附录

附件说明表，两列 `{LX}`，`\renewcommand{\arraystretch}{1.2}`。不写运行环境/硬件环境。

---

## 三、代码规范

### 绘图库

只允许 matplotlib，严禁 seaborn 及任何其他可视化库。

| 禁止 | 替代 |
|------|------|
| `sns.set_style()` | `plt.rcParams` + `ax.grid(alpha=0.3, linestyle='--')` |
| `sns.color_palette()` | `plt.cm.viridis(np.linspace(0.1, 0.9, n))` |
| `sns.heatmap()` | `ax.imshow()` + `plt.colorbar()` |
| `sns.despine()` | `ax.spines['top'].set_visible(False)` 等 |

### 两阶段执行（先算后画）

每个求解脚本必须按此顺序：

```
第一阶段：纯计算
  数据加载 → 预处理 → 建模 → 求解 → 得到所有数值结果

第二阶段：检查 + 绘图
   → 打印每组数据的 min/max/mean/std/CV/amplitude（供论文引用）
  → 每种分析结果都画图
```

### 绘图原则

每题至少 4-6 张图（覆盖主要分析维度），多多益善。数据和图的对应关系：

| 图表类型 | 适用场景 |
|----------|----------|
| 柱状图 | 类别对比、分组对比 |
| 折线图 | 趋势变化、时间序列 |
| 热力图 | 矩阵数据、相关性 |
| 散点图 | 两变量关系、分布 |
| 饼图 | 占比构成 |
| 箱线图 | 多组分布对比 |
| 通用 | 主要分析维度必画，多多益善 |

### 图表美观

- 配色：内置 colormap（viridis、RdBu_r、tab10、Set3）
- 折线图：线宽 ≥ 1.5pt，带标记点
- 柱状图：柔和色 + 白边 `edgecolor='white', linewidth=0.5`
- 散点图：`alpha=0.7`, `edgecolors='black', linewidth=0.5`
- 网格：`ax.grid(alpha=0.3, linestyle='--')`
- 去边框：`ax.spines['top'].set_visible(False)` + `right`
- **图上不画 `set_title()`**，标题由论文 `\caption{}` 承担
- 图内只允许：数据、坐标轴标签刻度、图例（全中文）

### 中文字体（跨平台）

```python
plt.rcParams['font.sans-serif'] = ['STHeiti', 'SimHei', 'Heiti TC',
    'Arial Unicode MS', 'Hiragino Sans GB', 'PingFang SC',
    'Microsoft YaHei', 'Songti SC', 'DejaVu Sans']
plt.rcParams['axes.unicode_minus'] = False
plt.rcParams['figure.dpi'] = 150
plt.rcParams['savefig.dpi'] = 150
plt.rcParams['savefig.bbox'] = 'tight'
```

### 公共头部

```python
import pandas as pd
import numpy as np
import matplotlib
matplotlib.use('Agg')
import matplotlib.pyplot as plt
import os, warnings
warnings.filterwarnings('ignore')

# 中文字体（见上方配置）
# ...

def despine(ax):
    ax.spines['top'].set_visible(False)
    ax.spines['right'].set_visible(False)

BASE_DIR = os.path.dirname(os.path.abspath(__file__))
FIG_DIR = os.path.join(BASE_DIR, '图片')
OUT_DIR = os.path.join(BASE_DIR, '结果')
os.makedirs(FIG_DIR, exist_ok=True)
os.makedirs(OUT_DIR, exist_ok=True)

def save_fig(fig, name_cn):
    fig.savefig(FIG_DIR + name_cn)
    plt.close(fig)

def save_csv(df, name_cn):
    df.to_csv(OUT_DIR + name_cn, index=False, encoding='utf-8-sig')
```

### 必要依赖

pandas, numpy, matplotlib, scipy, scikit-learn, chardet

禁止：seaborn, plotly, plotnine, altair, bokeh, holoviews

按需：xgboost, tensorflow, PyPDF2, python-docx

---

## 四、新题使用

1. 复制模板到新位置
2. 赛题放入 `题目/`（PDF或DOCX）
3. 数据放入 `数据/`
4. 可选：把解题思路、解题要求、老师补充说明、方法偏好或格式要求放入 `解题要求/`。没有额外要求时保持空文件夹即可。
5. 说"开始求解"

AI 自动：读题 → 计划 → 求解 → 论文 → 编译。
